spring.application.name=SolarPocket

# DATOS DIRECTOS DE CONEXIÓN (SIN VARIABLES)
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=UfuxASumrBwdpfEySJFBZcMboumOEtGS
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# THYMELEAF
# Nota: Cambiamos el prefijo si tus archivos están en la carpeta /presentation/
spring.thymeleaf.cache=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# LOGGING
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# SESIÓN Y MÉTRICAS
server.servlet.session.tracking-modes=cookie
solar.metrics.batch-size=5